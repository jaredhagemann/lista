{
  "name": "lista-monorepo",
  "private": true,
  "packageManager": "pnpm@10.29.2",
  "scripts": {
    "dev": "turbo run dev --filter=@lista/web",
    "build": "turbo run build --filter=@lista/web",
    "lint": "turbo run lint",
    "test:rls": "supabase db reset --db-url postgresql://postgres:postgres@127.0.0.1:54322/postgres && vitest run --config vitest.config.rls.mts tests/rls",
    "test:integration": "supabase db reset --db-url postgresql://postgres:postgres@127.0.0.1:54322/postgres && vitest run --config vitest.config.rls.mts tests/",
    "prepare": "husky"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.95.3",
    "dotenv": "^17.3.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.2",
    "supabase": "2.78.1",
    "turbo": "^2.5.4",
    "typescript": "^5",
    "vitest": "^4.0.18"
  },
  "lint-staged": {
    "apps/web/**/*.{ts,tsx}": "pnpm --filter @lista/web exec eslint --fix"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "^19.2.0"
    }
  }
}
